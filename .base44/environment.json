{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "JWT signing secret for auth tokens. Generated for development; replace with your own for production.",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    }
  ]
}
